<select id="doctor_hos_id" name="doctor[hospital_id]" onchange="doc_search_hos()" ><option value="">---请选择---</option>
<% if !@hospitals.nil? %>
    <% if !@hospitals.empty? %>
        <% @hospitals.each do |hos| %>
            <option id="serch_hos_<%= hos.id %>" value="<%= hos.id %>"><%= hos.name %></option>
        <% end %>
    <% end %>
<% end %>
</select>
<script>
    $(function() {
        $("#doctor_hos_id").select2()
    })
</script>