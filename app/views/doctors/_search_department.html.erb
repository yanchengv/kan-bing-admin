<select id="doctor_dep" name="doctor[department_id]" onchange="search_dep()" ><option value="">---请选择---</option>
<% if !@departments.nil? %>
    <% if !@departments.empty? %>
        <% @departments.each do |dep| %>
            <option id="serch_dep_<%= dep.id %>" value="<%= dep.id %>"><%= dep.name %></option>
        <% end %>
    <% end %>
<% end %>
</select>
<script>
    $(function() {
        $("#doctor_dep").select2()
    })
</script>