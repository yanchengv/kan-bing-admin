
<style>
   object{
       position: absolute;
       left: 50%;
       margin-left: -484px;
       background:#ffffff;
       margin-top:50px;

   }
   .title {
       color: #222;
       background: -ms-linear-gradient(top,#f5fafd,#ebf6f8);
       background: -moz-linear-gradient(center top, #f5fafd, #ebf6f8) repeat scroll 0 0 transparent;
       background: -webkit-linear-gradient(#f5fafd, #ebf6f8) repeat scroll 0 0 transparent;
       filter: progid:DXImageTransform.Microsoft.Gradient(gradientType=0, startColorStr=#f5fafd, endColorStr=#ebf6f8);
       line-height: 40px;
       padding: 0 10px;
       font-family: Arial,"Microsoft YaHei";
       font-size: 16px;
      border:1px solid #dcdcdc;
       position: absolute;
       left: 50%;
       margin-left: -585px;
       width:1170px;
       margin-top:-40px;
   }
   .bg{
       height:746px;background:#000;padding:0px;margin-top:50px;
   }
   @media (max-width: 1200px){
       .title{
           width:970px !important;
           position: absolute;
           left: 50%;
           margin-left: -485px;
           margin-top:-43px;

       }
       object{
           margin-top:0px;
       }
       .bg{
           height:726px;background:#000;padding:0px;margin-top:50px;
       }
   }
</style>
<div class="container bg" style="">
  <div class="title text-left" style="">
    <% @patient = Patient.where("id = ?", session["patient_id"]).first %>
    <strong><% @patient.nil? ? '' : @patient.name %>的超声视频</strong>
  </div>

  <div id="Playback" class="col-md-12">
  </div>

  </div>
  <script type="text/javascript">

      // Collect query parameters in an object that we can
      // forward to SWFObject:

      var pqs = new ParsedQueryString();
      var parameterNames = pqs.params(false);
      var parameters = {
//        src: "http://mediapm.edgesuite.net/strobe/content/test/AFaerysTale_sylviaApostol_640_500_short.flv",
//      src: "http://203.195.197.169:8082/playback.swf",
          src: "<%= @video_url%>",
          autoPlay: "true",
          verbose: true,
          controlBarAutoHide: "false",
          controlBarPosition: "bottom"
//        poster: "images/poster.png"
      };

      for (var i = 0; i < parameterNames.length; i++) {
          var parameterName = parameterNames[i];
          parameters[parameterName] = pqs.param(parameterName) ||
                  parameters[parameterName];
      }

      var wmodeValue = "direct";
      var wmodeOptions = ["direct", "opaque", "transparent", "window"];
      if (parameters.hasOwnProperty("wmode"))
      {
          if (wmodeOptions.indexOf(parameters.wmode) >= 0)
          {
              wmodeValue = parameters.wmode;
          }
          delete parameters.wmode;
      }

      // Embed the player SWF:
      swfobject.embedSWF(
              "/playback.swf"
              , "Playback"
              , 968
              , 726
              , "10.1.0"
              , "expressInstall.swf"
              , parameters
              , {
                  allowFullScreen: "true",
                  wmode: wmodeValue
              }
              , {
                  name: "Playback"
              }
      );



  </script>

